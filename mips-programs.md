# MIPS Programs

## Example Arithmetic

```assembly
add $t0, $t1, $t2
sub $t3, $t0, $t1

# Example Loop
loop:
addi $t0, $t0, 1
bne $t0, $t1, loop

Concepts Learned
Registers
Arithmetic operations
Branching
Memory access
Function calls

Purpose

Used to understand low-level processor operations.

